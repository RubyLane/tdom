pkg_dir=${pcfiledir}/../@PKG_DIR_BASENAME@
# tcl_pkg_path: the directory a Tcl interpreter should add to auto_path
# so that `package require @PACKAGE_NAME@` resolves to this install.
# Same as pkg_dir, exposed under a standardised name so consumers don't
# have to guess the per-package convention.
tcl_pkg_path=${pkg_dir}

Name: @PACKAGE_NAME@
Description: DOM/XML parsing, XPath, XSLT and JSON/HTML5 support for Tcl
Version: @PACKAGE_VERSION@
Cflags: -I${pkg_dir} -DUSE_TDOM_STUBS=1
Libs: -L${pkg_dir} -l@STUBLIB@
